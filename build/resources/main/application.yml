spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartfarmdb?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: 'root'
    password: '0136'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
  application:
    name: autofarmer
